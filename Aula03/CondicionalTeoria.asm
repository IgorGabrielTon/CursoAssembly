; If apenas compara sem nada inicialmente
; cmp EAX, EBX
; OPERADORES que saltam condicional
; (je) =
; (jg) >
; (jge) >=
; (jl) <
; (jle) <=
; (jne) !=
; GoTo (jmp) SALTO OBRIGATORIO
; Trazer números para os regeistradores usar DWORD[var]
; 
